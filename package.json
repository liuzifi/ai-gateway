{
  "name": "ai-gateway",
  "version": "1.0.0",
  "description": "Cloudflare Workers AI API Gateway",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test:types": "tsc --noEmit --project tsconfig.json",
    "test:syntax": "node .tmp/check-inline-js.js && node --check .tmp/inline.js",
    "test:quality": "npm run test:syntax && npm run typecheck && npm run test:metrics",
    "test:metrics": "node .tmp/quality-gate.mjs"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "ai",
    "proxy",
    "api-gateway"
  ],
  "license": "Apache 2.0",
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
